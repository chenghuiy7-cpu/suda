# IOPS测试方法

为了测试到最佳的IOPS，需要打开x86、ARM上的所有编译优化，并关闭所有调试输出。

测试前要检查以下几点：

## QDMA

确保driver/Makefile中的63行，DEBUG=0而不是1，之后重新编译

```bash
DEBUG=0 # 不是1
```

## NVMq

确保init_nvmq.sh的最后一行，不要开启dyndbg选项：

```bash
insmod nvmq.ko # dyndbg==pf
```

## SPDK